lib:
  build:
    image: lib
    dockerfile: Dockerfile
  cached: true

gcr_dockercfg:
  image: codeship/gcr-dockercfg-generator
  add_docker: true
  encrypted_env_file: scripts/gcr_cred.env.encrypted